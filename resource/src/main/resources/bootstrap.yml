spring:
  profiles:
    active: dev
  application:
    name: resource
feign:
  httpclient:
    enabled: true
    
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl